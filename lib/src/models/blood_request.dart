class BloodRequest{
  String address, bloodGroup, phoneNumber, userName, units, requestMessage;

  BloodRequest({this.requestMessage, this.address, this.bloodGroup, this.phoneNumber,
      this.userName, this.units});
}